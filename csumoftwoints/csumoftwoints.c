#include <stdio.h>

int main(){
	int score;
	int score2;
	int sum;

	score = 3;
	score2 = 5;
	sum = score + score2;

	printf("score is %d score2 is %d and sum is %d\n", score, score2, sum);
	return 0;
}
